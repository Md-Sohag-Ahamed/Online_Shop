
@{
  ViewData["Title"] = "AssignUserRole";
}

<h1>AssignUserRole</h1>
<br />
<br />
<div class="row">
  <div class="col-6">
    <h2 class="text-info">Assign User Role</h2>
  </div>
  @*<div class="col-6 text-right">
    <a asp-action="Create" class="btn btn-info"><i class="fa fa-plus-square" aria-hidden="true"></i>&nbsp;New Role</a>
  </div>*@
</div>
<br />
<div>
  <table class="table table-striped border">
    <tr class="table-info">
      <th>User Name</th>
      <th>Role Name</th>
      <th></th>
    </tr>
    @foreach (var item in ViewBag.UserRoles)
    {
      <tr>
        <td>@item.UserName</td>
        <td>@item.RoleName</td>


        <!--<td style="width:150px">
          <div class="btn-group">
            <a asp-action="Edit" class="btn btn-primary" asp-route-id=""><i class="fa fa-pencil-square-o" aria-hidden="true"></i></a>-->
            @*<a asp-action="Details" class="btn btn-success" asp-route-id="@item.Id"><i class="fa fa-list" aria-hidden="true"></i></a>*@
            <!--<a asp-action="Delete" class="btn btn-danger" asp-route-id=""><i class="fa fa-trash-o" aria-hidden="true"></i></a>

          </div>
        </td>-->
      </tr>
    }
  </table>
</div>

@section scripts{
  <script src="//cdn.jsdelivr.net/npm/alertifyjs@1.13.1/build/alertify.min.js"></script>
  <script type="text/javascript">
    $(function () {
      var save = '@TempData["save"]'
      if (save != null) {
        alertify.success(save);
      }
      var update = '@TempData["update"]'
      if (update != null) {
        alertify.success(update);
      }
      var deletedata = '@TempData["deletedata"]'
      if (deletedata != null) {
        alertify.success(deletedata);
      }
    })

  </script>
}




